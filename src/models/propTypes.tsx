export type IconProps = {
  name: string;
  className?: string;
  color?: string;
  width?: string;
  height?: string;
};

//for store reducer
export type TokenState = {
    tokenPrice: number,
    tokenName: string
 };

 